Run the program through Main.class

database is located in schema.sql

flightpackage is the source of all the classes and controllers we use.

Flight Search controller allows you to sign in/ create a user / log out
filter a list of flights and scroll through it
Book the selected flight,
select seats
and view your booking.
